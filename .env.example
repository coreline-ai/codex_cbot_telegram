# codex_cbot_telegram 텔레그램 봇 설정
# 이 파일을 복사하여 .env 파일을 생성하세요: cp .env.example .env

# 텔레그램 봇 토큰 (BotFather에서 발급)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# 허용할 사용자 ID (쉼표로 구분하여 여러 명 가능)
TELEGRAM_ALLOWED_USERS=your_user_id_here

# 폴링 간격 (초) - 기본값: 10초
TELEGRAM_POLLING_INTERVAL=10

# OpenAI API Key (선택 사항)
# Codex CLI는 'codex login' 기반의 OAuth를 사용하므로
# 코어 작동 및 이미지 생성(브라우저/도구 활용 시)에 키가 필요하지 않을 수 있습니다.
# OPENAI_API_KEY=

# macOS strict mode (bash+executor.sh only)
# 1: direct codex fallback disabled
# 0: allow fallback (useful for Windows test)
MACOS_STRICT_MODE=0

# Image generation provider chain
# auto: codex_cli -> sd_webui -> stock -> canvas
# codex_cli: force codex-based image subtask
# sd_webui: force local Stable Diffusion WebUI API
# stock: force stock-photo fetch fallback (LoremFlickr)
# canvas: force deterministic local canvas fallback
IMAGE_GEN_PROVIDER=auto
CODEX_EXE=
# Leave blank to use codex CLI default model/profile.
CODEX_IMAGE_MODEL=
# Optional comma-separated fallback models tried in order when access/model errors occur.
# Example: gpt-5-codex,gpt-5
CODEX_IMAGE_MODEL_CANDIDATES=
# 1 to bypass nested codex sandbox/approval for image worker calls (use carefully).
CODEX_IMAGE_BYPASS_SANDBOX=0
# Nested codex sandbox mode when bypass is off.
CODEX_IMAGE_SANDBOX=workspace-write
# 1 to allow shell/tool usage if native image path is unavailable.
CODEX_IMAGE_ALLOW_SHELL_TOOLS=0
CODEX_IMAGE_TIMEOUT=300
STOCK_IMAGE_TIMEOUT=25
STOCK_IMAGE_WIDTH=1600
STOCK_IMAGE_HEIGHT=1200
STOCK_IMAGE_RETRIES=3
WEB_PREVIEW_BASE_URL=http://127.0.0.1:8080/api/files
WEB_DIVERSITY_MODE=balanced
# Optional fixed seed for reproducible variation picks.
WEB_VARIATION_SEED=
